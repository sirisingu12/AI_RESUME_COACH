"""
tests
-----
Test suite for the AI Resume Analyzer & Career Coach.

Run all tests from the project root with:

    python -m pytest tests/ -v

or with unittest:

    python -m unittest discover -s tests -v

Tests that require the embedding model to be downloaded are mocked
by default. Tests that require LM Studio to be running are skipped
unless explicitly enabled.
"""
